---
aside: 3
asideBreak: xl
asideMobile: false
---
{% extends './main.njk' %}

{% block content %}

    <section class="print-container p-3 mb-3">
        <h1>{{ title }}</h1>
        {# need to load full page data #}
        {% set post = collections[globals.posts.collectionName] | getCollectionItem(page) %}
        {% if post.data.image %}<img src="{{ post.data.image | url }}" class="w-25"></img>{% endif %}
        {{ content | safe }}
    </section>
  
{% endblock %}

{% block aside %}

{% endblock %}
