---
layout: page
asideContentClass:
  recent: order-0
  tagCloud: order-1
  toc: d-none
title: Tags cloud
permalink: /tag-cloud/
---
<div class="taxonomy tag-cloud">
  
  <div class="heading">
    Tag Cloud
  </div>

  <div class="cloud">
    {% for tag in collections.tagCollection %} 
    <span style="font-size: {{ tag.size }}em;">
        <a href="{{ globals.tag.path | url | normalize }}{{ tag.name | slug | firstPage(globals.tag.postsPageSize) }}">{{ tag.name }}</a>
    </span>
    {% endfor %}
  </div>

</div>